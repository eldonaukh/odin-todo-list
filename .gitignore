# --- Dependencies ---
# Never check in node_modules. It's huge and can be recreated via 'npm install'.
node_modules/
jspm_packages/

# --- Build Output ---
# Webpack's default output directory
dist/
# Alternative build folder names
build/
out/
# If you generate source maps in separate files
*.map

# --- Logs ---
# Error logs generated by npm or yarn
npm-debug.log*
yarn-debug.log*
yarn-error.log*
lerna-debug.log*

# --- Environment Variables ---
# Contains API keys and secrets. NEVER commit these.
.env
.env.local
.env.development.local
.env.test.local
.env.production.local

# --- Editor / OS Specific ---
# macOS
.DS_Store
# Windows
Thumbs.db
# IDEs (VS Code, WebStorm, etc.)
.vscode/
.idea/
*.suo
*.ntvs*
*.njsproj
*.sln

# --- Testing ---
# Coverage reports generated by tools like Jest
coverage/
*.lcov

# --- Optional: TypeScript ---
# If you use TypeScript, you might want to ignore the build info
*.tsbuildinfo